import SpirareBrowserQRImage from '@site/src/components/EmbeddedContent/img/spirare-browser-arfoundation-marker_qr-detection.png'

カメラ正面に QR コードが映るようにして、数秒間待ってください。  
その後端末を少し左右に動かすと、QR コードが認識されます。

<img width="480" class="border" src={SpirareBrowserQRImage} />